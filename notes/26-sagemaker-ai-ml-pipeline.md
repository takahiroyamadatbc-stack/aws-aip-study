# Amazon SageMaker AI と機械学習パイプライン

## MLパイプラインの流れ

機械学習のワークフローは、以下の5段階を順に進む。

```
① データ収集 → ② データの前処理と分析 → ③ モデルトレーニング → ④ モデル評価 → ⑤ モデルのデプロイと推論
```

| # | 段階 | 概要 |
|---|------|------|
| ① | データ収集 | 下記参照 |
| ② | データの前処理と分析 | 下記参照 |
| ③ | モデルトレーニング | (追記予定) |
| ④ | モデル評価 | (追記予定) |
| ⑤ | モデルのデプロイと推論 | (追記予定) |

## ① データ収集

モデルの精度は「どれだけ良いデータを集められたか」で上限が決まる（Garbage In, Garbage Out）。収集段階では以下の観点を検討する。

| 観点 | 何を考えるか | 不十分だと何が起きるか |
|------|--------------|--------------------------|
| **関連性（Relevance）** | 解きたいタスク・予測したい対象と、収集するデータが実際に関係しているか | タスクと無関係な特徴量ばかり集めても学習に寄与せず、ノイズとして精度を下げる |
| **量（Quantity）** | モデル（特に深層学習・複雑なモデル）を十分に学習させられるだけのデータ件数があるか | データ不足は過学習（Overfitting）を招き、未知データへの汎化性能が下がる |
| **品質（Quality）** | 欠損値・誤記・重複・ラベル誤りなどが少なく、正確に測定・記録されたデータか | 品質の低いデータで学習すると、モデルは誤ったパターンを「正解」として学習してしまう |
| **バイアス（Bias）** | 特定の属性・集団・条件に偏ったデータになっていないか（収集元・収集期間・収集方法に偏りがないか） | 特定グループへの予測精度が著しく低くなる、公平性を欠く（Responsible AIの観点でも問題になる。関連: [31-responsible-ai-core-dimensions.md](31-responsible-ai-core-dimensions.md)） |
| **コストと時間（Cost and Time）** | データ収集・ラベリング・権利処理（ライセンス・プライバシー同意）にかけられる予算と期間はどれだけか | 網羅的な収集を追求しすぎるとプロジェクトが停滞する。実務では精度要件とのトレードオフで収集範囲を決める |

収集したデータをどの形式（CSV/JSON/Avro/Parquet/ORC）で保管・受け渡しするかの比較・使い分けは[10-data-formats.md](10-data-formats.md)を参照。

## ② データの前処理と分析

### 探索的データ分析（EDA）

前処理の方針（どの列にどの変換を当てるか）を決める前段として、分布・欠損・相関・不均衡を定量的に把握する工程。

| チェック項目 | 見るもの | 何につながるか |
|------|------|------|
| 記述統計 | 平均・中央値・分散・四分位範囲 | スケーリング方針（正規化 vs 標準化）の判断材料 |
| 欠損パターン | 欠損率、MCAR/MAR/MNARのどれか | 削除で済むか、補完が必要か、補完方法の選定 |
| 分布の形状 | 歪度（skewness）、多峰性、外れ値 | ログ変換・ビニングの要否判断 |
| 相関・多重共線性 | 特徴量間の相関係数、VIF | 冗長な特徴量の削除、次元削減の要否 |
| クラス不均衡 | ターゲット変数の分布 | リサンプリング（SMOTE等）や評価指標選定（Accuracy不適切→F1/AUC） |

AWSでは**SageMaker Studio の Data Wrangler**が中心的なツールで、データ取り込み時に自動生成される「Data Quality and Insights Report」で欠損率・相関ヒートマップ・ターゲットリーケージ候補・クラス不均衡を一括可視化できる。バイアスの定量評価（Class Imbalance、Difference in Proportions of Labels等の**学習前バイアスメトリクス**）は**SageMaker Clarify**が担当領域で、これはEDA段階（モデルを学習する前）に実施する点が試験の狙われどころ。学習後バイアス（post-training bias）や説明可能性（SHAP値）はモデル評価段階のClarifyの役割で別物（詳細は[31-responsible-ai-core-dimensions.md](31-responsible-ai-core-dimensions.md)）。

### データの前処理

#### データクレンジング

- **欠損値処理**: 欠損率が低ければ行/列削除、高ければ補完（平均・中央値・最頻値の単純補完、KNN/MICEのようなモデルベース補完）。Data Wranglerには"Handle missing"変換がビルトインで用意されている
- **重複除去**: 完全重複だけでなく、キーの表記揺れによる実質重複（ファジーマッチ）も対象になりうる
- **外れ値処理**: IQR法・Zスコア法で検出し、除去またはWinsorization（上下限でキャッピング）。外れ値がドメイン上意味のある事象（不正検知の異常値等）なら安易に除去しない
- **表記・型の統一**: 日付フォーマット、単位、大文字小文字などの不整合を統一

#### カテゴリカルデータの数値化

| 手法 | 向いているデータ | 注意点 |
|------|------------------|--------|
| One-Hot Encoding | 順序のない名義変数（nominal）、カーディナリティが低い | カーディナリティが高いと次元が爆発する（スパース化） |
| Label/Ordinal Encoding | 順序のある変数（ordinal） | 順序のない変数に使うと、モデルが数値の大小関係を誤って学習するリスク（ただし木ベースモデルは分割点ベースで動くため実用上問題になりにくい） |
| Target/Mean Encoding | 高カーディナリティの名義変数 | ターゲットリーケージに直結しやすく、CVのfold内で計算する等リーク対策が必須 |

#### 正規化・標準化

- **正規化（Normalization / Min-Max Scaling）**: 値を[0, 1]等の範囲にスケールする。外れ値の影響を受けやすい
- **標準化（Standardization / Z-score）**: 平均0・分散1に変換する。正規化よりは外れ値に頑健
- **使い分け**: 距離ベース（KNN, SVM, k-means）や勾配降下法ベース（ニューラルネット、ロジスティック回帰）のアルゴリズムはスケールの影響を強く受けるため必須。決定木・ランダムフォレスト・勾配ブースティング系（XGBoost等）は分割点ベースでスケール不変なので基本的に不要
- **リーク対策**: fit（パラメータ算出）は必ず学習データのみで行い、テストデータにはそのパラメータでtransformのみを適用する

#### ビニング（Binning / Discretization）

- **等幅（Equal-width）**: 値域を均等な幅で区切る。分布が偏っているとビンごとの件数が不均衡になりやすい
- **等頻度（Equal-frequency / Quantile）**: 各ビンに同数のサンプルが入るよう区切る
- **目的**: ノイズ・外れ値の影響緩和、非線形な関係の捕捉、木モデルでの分割点削減による解釈性向上

#### ログ変換

- **目的**: 右に歪んだ（右裾が長い）分布を正規分布に近づける、分散を安定化させる、乗法的な関係を加法的（線形）な関係に変換する
- **ゼロ・負値への対応**: `log(x)`はx=0で発散するため`log1p`（`log(1+x)`）を使う、あるいはBox-Cox/Yeo-Johnson変換（負値も扱える一般化版）を検討する

### 特徴量エンジニアリング

- **特徴量生成**: 交互作用項・多項式特徴量、日時の分解（曜日・月・季節性フラグ）、集約特徴量（過去N日の移動平均等）、ドメイン知識に基づく派生変数
- **特徴量選択**: フィルタ法（相関係数・カイ二乗検定・相互情報量で単変量にスコアリング）、ラッパー法（RFEのようにモデル性能を見ながら反復的に選択）、埋め込み法（Lasso正則化の係数、木モデルのfeature importanceで選択）
- **次元削減**: PCA等。多重共線性の緩和・過学習抑制・学習の高速化に寄与するが、変換後の軸は解釈性を失う点がトレードオフ

**SageMaker Feature Store**は、前処理・特徴量エンジニアリングの成果物（特徴量）をチーム・パイプライン横断で再利用するためのリポジトリ。Online Store（低レイテンシのリアルタイム推論用ルックアップ）とOffline Store（S3 + Glue Data Catalog上の、学習・バッチ推論用の履歴データ）の2種類を持ち、同じ特徴量定義を学習時と推論時の両方で参照することで**training-serving skew**（学習時と推論時で特徴量の計算方法がズレる問題）を防ぐのが最大の存在意義。Data Wranglerで作った変換フローはそのままFeature Storeへのエクスポート、またはSageMaker Processing Jobでのスケーラブルな実行（scikit-learnコンテナ/Sparkコンテナ）につなげられる。

### 試験でのひっかけポイント整理

- 「正規化と標準化は同じ意味である」→ 誤り。正規化は値域を[0,1]等に収める（Min-Max）、標準化は平均0・分散1にする（Z-score）。範囲を保証するのは正規化の方
- 「決定木系モデルにも正規化・標準化が必須」→ 誤り。決定木・ランダムフォレスト・勾配ブースティングは分割点ベースでスケール不変なため、基本的にスケーリング不要（距離ベース・勾配ベースのモデルとの対比を問われやすい）
- 「Target Encodingは高カーディナリティなカテゴリ変数に安全に使える」→ 要注意。ターゲットリーケージのリスクがあり、CVのfold内で統計量を計算するなどの対策が前提
- 「SageMaker Clarifyのバイアス検出はモデル学習後にしか使えない」→ 誤り。Clarifyは学習前（pre-training bias、EDA段階でのクラス不均衡等の検出）と学習後（post-training bias）の両方をカバーする
- 「Feature StoreのOnline StoreとOffline Storeはどちらも同じ用途」→ 誤り。Online Storeは推論時の低レイテンシ参照、Offline Storeは学習・バッチ処理向けの履歴データ蓄積、と役割が異なる
- 「対数変換はどんな分布にも無条件に適用できる」→ 誤り。0や負値を含む場合は`log1p`やBox-Cox/Yeo-Johnson等の工夫が必要
