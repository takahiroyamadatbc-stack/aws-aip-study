# SageMaker AI: モデルのデプロイと推論

MLパイプライン全体の位置づけは[260-sagemaker-ml-pipeline-overview.md](260-sagemaker-ml-pipeline-overview.md)を参照。

評価済みモデルを実際に予測結果を返せる状態にする工程。**「どこにデプロイするか（デプロイ先）」**と**「推論のリクエストパターン（リアルタイム/バッチ）」**は別軸の検討事項で、SageMaker Endpointを使う場合でも下記のリアルタイム/バッチの選択は必要になる。

## デプロイ先の選択肢

| デプロイ先 | 概要 | 主なAWSサービス・技術 | デプロイ先の例 | 向いているケース |
|------|------|------|------|------|
| **SageMaker Endpoint（マネージドホスティング）** | AWSが管理するホスティングインフラ上にモデルをデプロイ。インスタンスの起動・スケーリング・パッチ適用等の運用をAWS側が担う | SageMaker Hosting（EC2ベースのマネージドインフラ） | リアルタイムAPI、社内向け推論サービス、既存アプリからのAPI呼び出し | 運用負荷をかけずに素早く本番稼働させたい、Auto Scaling等のマネージド機能をそのまま使いたい場合 |
| **自己管理型インスタンス（Self-managed）** | ユーザー自身がコンピューティング基盤を用意し、モデルサーバーを自前で構築・運用する | EC2、ECS、EKS（Dockerコンテナ）、TorchServe/TensorFlow Serving/Triton Inference Serverなどのモデルサーバー | 既存のマイクロサービス基盤（ECS/EKSクラスタ）への統合、特殊なランタイム・ライブラリが必要な推論処理、オンプレミスサーバー | 既存のコンテナ運用基盤に合わせたい、SageMakerがサポートしない特殊要件がある、コスト・構成を細かく自前で最適化したい場合 |
| **エッジ環境（Edge）** | クラウドと通信せず、デバイス側でモデルを実行する | **SageMaker Neo**（ターゲットのエッジハードウェア向けにモデルをコンパイル・最適化）＋ **AWS IoT Greengrass**（デバイスへのモデル配布・ライフサイクル管理） | 工場のカメラ・センサー端末、自動運転車載機、スマートカメラ、産業用ロボット、船舶・僻地のIoT機器 | ネットワーク接続が不安定/オフラインでも動作させたい、通信レイテンシを許容できない、クラウドへのデータ送信自体を避けたい（プライバシー・帯域制約）場合 |

## リアルタイム推論（Real-time Inference）

- **SageMaker Endpoint**としてモデルをデプロイし、常時起動のHTTPSエンドポイントに1件ずつリクエストを送って低レイテンシで結果を受け取る
- インスタンスは常時起動＝**稼働している間ずっと課金**される。Auto Scalingでインスタンス数を需要に応じて増減できる
- 用途: レコメンド、不正検知、チャットボットなど**即時応答が必要**なユースケース
- 稼働中のエンドポイントを新バージョンへ安全に切り替える機構（**Deployment Guardrails**によるCanary/Linear/All at onceのトラフィックシフトと自動ロールバック）は、他のAIサービスのデプロイ機構と横断比較した[201-ai-service-deployment-strategies.md](201-ai-service-deployment-strategies.md)を参照

## 大規模言語モデル（LLM）のデプロイ: SageMaker LMIコンテナ

数十億〜数百億パラメータ級のオープンソースLLM（Llama、Mistral等）をリアルタイム推論エンドポイントにデプロイする場合、通常のモデルデプロイでは想定しない固有の課題が出てくる。汎用のGPUインスタンス・標準的なデプロイ設定のまま持ち込むと、コンテナが起動直後にメモリ不足でクラッシュする、といった失敗が典型的に起きる。

**大規模モデル特有の課題**

| 課題 | 内容 |
| --- | --- |
| GPUメモリの絶対的な不足 | 700億パラメータのモデルはFP16精度で重みだけで約140GBに達する。単一GPU（例: ml.g5.xlargeの24GB）には到底収まらない |
| モデルダウンロードの時間 | モデルアーティファクトが数十GB〜になると、S3からのダウンロードだけで既定のタイムアウトを超えうる |
| コンテナ起動のヘルスチェック | 大容量モデルのロードに時間がかかるため、既定のヘルスチェックタイムアウト内に起動が完了せず「起動失敗」と判定されることがある |

**解決策: Large Model Inference（LMI）コンテナ**

SageMakerが提供する**LMIコンテナ**は、大規模LLM推論専用に設計された推論コンテナ（DJLServing・vLLM・TensorRT-LLM等のバックエンドを内包）。単に大きいGPUインスタンスを使うだけでなく、以下の技術で「単一GPUに収まらないモデルを、低レイテンシーを保ったまま動かす」ことを実現する。

| 技術 | 何をするか |
| --- | --- |
| **テンソル並列化（Tensor Parallelism）** | モデルの重み（各層の行列）を複数GPUに分割してロードする。GPUメモリの合計を実質的な上限として扱えるようになり、単一GPUのメモリ上限を超えたモデルを運用できる |
| **量子化（Quantization）** | 重みの精度をFP16→INT8/INT4等に落とし、GPUメモリ使用量そのものを削減する（テンソル並列化と併用が一般的） |
| **継続バッチング（Continuous Batching）** | リクエストをリクエスト単位ではなくトークン生成単位で動的にバッチ処理し、GPU使用率とスループットを高める |

**起動失敗を防ぐエンドポイント設定**

大規模モデル向けには、デプロイ時に以下のパラメータを既定値から引き上げる必要がある。

| パラメータ | 役割 |
| --- | --- |
| `VolumeSizeInGB` | エンドポイントインスタンスにアタッチするEBSボリュームサイズ。モデルアーティファクトの展開・キャッシュに十分な容量を確保する |
| `ModelDataDownloadTimeoutInSeconds` | S3からモデルアーティファクトをダウンロードする際のタイムアウト。既定値のままだと大容量モデルでタイムアウトしうる |
| `ContainerStartupHealthCheckTimeoutInSecondsInSeconds` | コンテナ起動〜ヘルスチェック成功までの許容時間。モデルロードに時間がかかる大規模モデルでは延長が必須 |

**誤って選びがちな他の選択肢との比較**

| 選択肢 | 何が問題か |
| --- | --- |
| SageMakerサーバーレス推論 | メモリ上限が6,144MB（6GB）で、大規模LLMの重みには全く足りない。加えて**GPUがFeature exclusionsで明示的に除外**されており、GPU加速推論自体が使えない |
| 単一GPUの汎用GPUインスタンス＋水平スケールアウト、モデルをtar.gz圧縮 | 水平スケールアウトは**各インスタンスが独立してモデル全体をロードする**構成のため、インスタンス数を増やしても1台あたりのGPUメモリ不足は解消されない（垂直方向の制約と水平方向のスケーリングを混同している）。また大規模モデルのtar.gz圧縮・展開は時間・ディスクを浪費するため、非圧縮でのデプロイが推奨される |
| AWS Lambda（コンテナイメージ） | 関数メモリの上限は10,240MB（10GB）で大規模LLMには不足する上、**LambdaはGPUを提供しない**（メモリに比例したvCPUのみ）。GPU加速というリアルタイム推論の要件をそもそも満たせない |

## バッチ推論（Batch Inference）

- **SageMaker Batch Transform**として、S3上に置いた大量データに対しまとめて非同期で推論を実行するジョブ型の仕組み
- ジョブ実行中だけインスタンスが起動し、**処理が終われば自動的に停止**する（常時起動のエンドポイントは不要）ため、課金は実行時間分のみ
- 用途: 夜間バッチでの一括スコアリング、レポート生成など**即時応答が不要**なユースケース

## エッジデプロイ: SageMaker Neo + AWS IoT Greengrass

クラウド上のSageMaker Endpointとは別に、**デバイス側でモデルを実行する**デプロイ先。ネットワークが不安定/オフラインでも動かしたい、通信レイテンシを許容できない、生データをクラウドに送りたくない（プライバシー・帯域制約）といった要件で選ぶ。この構成は**SageMaker Neo（モデルの最適化・コンパイル）**と**AWS IoT Greengrass（デバイスへの配布・実行管理）**という、役割の異なる2つのサービスの組み合わせで成り立っており、どちらか一方だけでは完結しない点が試験・実務双方での理解のポイント。

**SageMaker Neo（コンパイルステップ）**

**解決する課題**: クラウド学習時に使うTensorFlow/PyTorch/MXNet/ONNX/XGBoost等のモデルは、汎用的なフレームワーク実行環境（Python + フレームワークランタイム一式）を前提に動く。これをそのままエッジデバイス（CPU/メモリが限られ、フレームワーク自体が乗らないこともある組み込み機器）に持ち込むと、動作が重い・そもそも実行できない、という問題が起きる。Neoは、学習済みモデルを**ターゲットとなるハードウェア専用の実行形式にコンパイル・最適化する**サービス。

- **何をしているか**: モデルの計算グラフを解析し、演算子の融合（operator fusion）やターゲットのCPU/GPU命令セットに合わせた最適化を行い、フレームワーク非依存の軽量な実行形式に変換する（内部的にはApache TVMベースのコンパイラ技術を利用）。これにより推論速度の向上とメモリ使用量の削減を、精度をほぼ落とさずに実現する
- **対応ターゲット**: ARM/Intel/NVIDIA系のCPU・GPU、Ambarella等のカメラ向けチップから、クラウド上のEC2インスタンス・SageMaker Endpointまで、多様なハードウェアプロファイルを指定できる（Neoは「エッジ専用」ではなく、クラウド側インスタンスの推論高速化にも使える点に注意）
- **出力**: コンパイル済みモデルアーティファクトと、それを実行するための軽量ランタイムである**Neo Deep Learning Runtime（DLR）**のペア。DLRはOSSとして公開されており、コンパイル済みモデルをロードして推論を実行する最小限のランタイム
- **Neoが担わないこと**: コンパイルまでが仕事の範囲で、コンパイル済みモデルを実機にどう配って・どう更新して・どう動かし続けるかという運用面は担当しない

**AWS IoT Greengrass（配布・実行管理ステップ）**

**解決する課題**: コンパイル済みモデルができても、それを何百〜何千台という単位のデバイスフリートに配布し、バージョン更新し、オフライン時も含めて動かし続けるには、デバイス側のエージェント・ライフサイクル管理・クラウドとの同期の仕組みが別途必要になる。Greengrassは、エッジデバイス上でAWSクラウドの機能の一部（Lambda実行、ML推論、メッセージング等）を**ローカルで**動かせるようにするIoT向けのエッジランタイム/オーケストレーションサービス。

- **Components（コンポーネント）**: 推論ロジック・MLモデル・データ前処理コードなどをComponentという単位でパッケージ化し、デバイスへデプロイ・バージョン管理する。GreengrassにはNeo Deep Learning Runtime用のコンポーネントが用意されており、Neoでコンパイル済みのモデルをそのままデバイスフリートへ配布・実行できる
- **デバイスフリート管理**: 対象デバイスをグループ化し、Componentの一括デプロイ・ロールバック・段階的な展開（Canary的な展開）を行える
- **オフライン耐性とクラウド同期**: 通常時はAWS IoT Core（MQTT）経由でクラウドとメッセージ・状態を同期するが、ネットワークが切断されてもデバイス上でローカルに推論を継続できる。接続が復旧すれば同期を再開する
- **ローカルでの後処理**: Greengrass上で動くLambda関数と組み合わせ、推論結果のローカルでのフィルタリング・集約・アクション（アラート発報等）を行い、クラウドへは必要な情報だけを送る、という構成もよく使われる

**役割分担のまとめ**

| サービス | 担当する工程 | やらないこと |
|------|------|------|
| SageMaker Neo | 学習済みモデルをターゲットハードウェア向けに**コンパイル・最適化**する | デバイスへの配布・バージョン管理・実行時のライフサイクル管理 |
| AWS IoT Greengrass | コンパイル済みモデルをデバイスフリートへ**配布・実行・継続運用**する（オフライン耐性、OTA更新、ローカル処理を含む） | モデル自体の最適化・コンパイル（Neoが生成した成果物を受け取って動かす側） |

「クラウドで学習したモデルを、ネットワーク不安定なエッジ環境で低レイテンシに動かし続ける」というユースケースは、Neoによる最適化とGreengrassによる配布・運用管理の**両方があって初めて完結する**。SageMaker Endpointのような単一サービスでの完結型ではない点が、他のデプロイ先との一番の違い。

## 使い分けの軸

| 観点 | リアルタイム推論 | バッチ推論 |
|------|-------------------|-------------|
| レイテンシ要件 | 即時応答が必要 | 応答まで時間がかかってもよい |
| リクエストの単位 | 1件ずつ | まとめて大量データ |
| インスタンス稼働 | 常時起動（Auto Scaling可） | ジョブ実行中のみ起動、終わったら停止 |
| 課金 | 稼働時間に応じて継続課金 | ジョブ実行時間分のみ |

上記2つ以外にも、リクエストが不定期・低頻度で**アイドル時は課金を止めたい**場合の**Serverless Inference**、大きなペイロード・長い処理時間でキューイングしたい場合の**Asynchronous Inference**といった選択肢もあるが、まずは「即時応答か・まとめて処理でよいか」でリアルタイム推論とバッチ推論のどちらを軸に考えるかを判断するのが基本。

## インスタンス選定支援: SageMaker Inference Recommender と AWS Compute Optimizer

どちらも「推論インフラのインスタンスを適切にサイジングする」ためのツールだが、対象範囲とタイミングが異なる。

| 観点 | SageMaker Inference Recommender | AWS Compute Optimizer |
|------|-----------------------------------|--------------------------|
| 対象範囲 | **SageMaker推論エンドポイント専用** | EC2、Lambda、EBS、ECS on Fargate、RDS、**SageMaker推論エンドポイント**を含む、**AWS全体のリソース最適化サービス** |
| 実行タイミング | **デプロイ前**（新規にどのインスタンスタイプ・構成で捌くべきかを事前に決める） | **稼働中のリソース**の実績データを継続的に分析する（デプロイ後） |
| 手法 | 実際にモデルを複数のインスタンスタイプへロードし、**負荷テスト**を実行してレイテンシ・スループット・コストを比較 | CloudWatchの**過去の使用率メトリクス**（CPU/メモリ使用率等、通常14日以上）を機械学習ベースで分析し、過剰/過小プロビジョニングを検出 |
| モード | Instance Recommendation（既定の負荷で簡易に候補を絞る、約45分）／Load Test（自分のトラフィックパターンでカスタム負荷テスト、より詳細） | 常時分析（追加のジョブ実行は不要、有効化するだけで継続的にレコメンドが更新される） |
| 出力 | 推奨インスタンスタイプ・推奨エンドポイント構成、レイテンシ/スループット/コストのトレードオフ一覧 | リソースごとのrightsizing推奨（インスタンスタイプ変更等）とコスト削減見込み |
| 主な用途 | **新規デプロイ時**に、どのインスタンスタイプ・インスタンス数で構成するのが最適かを決める | **既存の稼働中リソース全般**（SageMakerエンドポイントも含む）を継続的に見直し、無駄なコストを削減する |

**使い分けの基本方針**: 新規にエンドポイントをデプロイする前の「どのインスタンスで始めるか」の意思決定にはInference Recommender、デプロイ後の運用フェーズで「実際の使用率に対してインスタンスが過剰/過小でないか」を継続的に見直すにはCompute Optimizer、という時系列の関係で使い分ける（併用も可能）。

## 試験でのひっかけポイント整理

- 「バッチ推論もリアルタイム推論も常時起動のエンドポイントが必要」→ 誤り。バッチ推論（Batch Transform）はジョブ実行中のみインスタンスが起動し、終了後は自動的に停止する。常時起動が必要なのはリアルタイム推論（Endpoint）の方
- 「エッジデバイスへのデプロイもSageMaker Endpointの一種である」→ 誤り。エッジデプロイはSageMaker Neo（モデルのコンパイル・最適化）とAWS IoT Greengrass（デバイスへの配布・管理）の組み合わせで実現するもので、クラウド上のマネージドホスティングであるSageMaker Endpointとは別の仕組み
- 「SageMaker Neoだけでエッジデバイスへのモデル配布・運用まで完結する」→ 誤り。Neoが担うのはターゲットハードウェア向けのモデルの**コンパイル・最適化**までで、デバイスフリートへの配布・バージョン管理・オフライン時の継続実行はAWS IoT Greengrassの役割。両者は別サービスの分業であり、どちらか一方では実運用のエッジ推論は完結しない
- 「AWS Compute OptimizerはEC2等インフラ全般が対象で、SageMakerの推論エンドポイントは対象外」→ 誤り。Compute OptimizerはSageMaker推論エンドポイントも分析対象に含む
- 「Inference RecommenderとCompute Optimizerはどちらか一方で十分」→ 誤り。Inference Recommenderはデプロイ前の初期インスタンス選定、Compute Optimizerはデプロイ後の継続的な見直しと、担うタイミングが異なるため併用しうる
