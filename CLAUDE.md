# CLAUDE.md

このリポジトリでの作業ルール。プロジェクトの目的・ディレクトリ構成・Labの作り方の基本は [README.md](README.md) を参照。ここには**Claudeが作業時に守るべき挙動**を書く。

## 対象試験について

このリポジトリの学習対象は **AWS Certified Generative AI Developer - Professional（試験コード: AIP-C01）**。AWS Certified AI Practitioner（試験コード: AIF-C01、Foundationalレベルの別資格）とは試験コードが紛らわしい（AIP-C01 / AIF-C01）上に対象者レベルも異なるため、**絶対に混同しないこと**。`notes/`のドメイン番号帯（`10`〜`59`）は、AIP-C01公式試験ガイドのContent Domain 1〜5（Foundation Model Integration, Data Management, and Compliance / Implementation and Integration / AI Safety, Security, and Governance / Operational Efficiency and Optimization for GenAI Applications / Testing, Validation, and Troubleshooting）に対応している。

## 学習の進め方（基本サイクル）

このリポジトリでの標準的なやり取りは以下の流れ。

1. ユーザーが、間違えた問題文・選択肢・自分が選んだ答えとその理由をそのまま貼る
2. 問題自体に解説が付いていることもあるが、ユーザーはそれをなぞるのではなく、**自分がさらに深掘りしたい点／実際に手を動かして確認したい点／まだ触ったことがない機能**を言及する
3. Claudeはその言及を踏まえて、サンプルコード（CLI/CDK/SDKなど）の提示や解説を行う

この1〜3のやり取りがそのままLabの`問題／選んだ答えと理由／誤答の型／仮説`の材料になる。会話がまとまってきたら「Labの作り方」に沿ってLab化し、結論・片付けまで進める（実機でのAWSデプロイ・実行は標準では行わない。下記「AWS実行について」参照）。ユーザーから明示的にLab化の指示がなくても、深掘り・検証の流れが一区切りついたタイミングでLab化を提案してよい。

誤答の性質によってLab化するか`notes/`への知識整理に留めるかを使い分ける。**用語・仕組みを知らなかった／背景知識が不足していた**という知識問題よりの誤答は、実機検証を伴うLab化を提案せず`notes/`配下への知識整理を優先する（詳細は「メモを取るとき」を参照）。一方、**実際に動かしてみないと挙動が確定しない仕様の誤解**や、手順・設定次第で結果が変わる内容は、実機検証の材料としてLab化する。

## AWS実行について

- Lab作成は標準では**コード（IaC/CLIスクリプト）とREADMEの結論整理まで**を完了条件とし、実際にAWS上へデプロイ・実行することはしない
- ユーザーから「実行して」「デプロイして」「確認して」のように明示的な指示があった場合のみ、実機デプロイ・検証・片付けまで行う
- 実機実行しない場合、READMEの「検証」セクションはコマンドの実行結果ではなく、スクリプト/コードの意図と期待される挙動として記述する
- 実機実行した場合は、必ず片付け（`cdk destroy` / `sam delete` / `0N_delete_xxx.sh`）まで行う（「作業終了時」参照）

## Labを作るとき

- Labの型（IaC型 / CLIスクリプト型）はREADMEの「Labの作り方」を参照し、内容に応じて選ぶ。CDK/CFnでスタックを組むほどの規模でなければCLIスクリプト型（`01_create_xxx.sh` → `0N_delete_xxx.sh`）にする
- 連番（NNN）を決める前に `labs/` を一覧して既存の最大値を確認する。既存Labの番号・リソース名とズレないよう注意する
- スタック名・S3バケット名・DynamoDBテーブル名など、AWS上に作るリソース名は必ず `aip-NNN-topic` 形式にし、Labのフォルダ番号と一致させる。フォルダ番号とリソース名の番号がズレていないか作成前に確認する
- デプロイ・作成コマンドには必ず `--tags Project=aip-study`（相当のタグ指定）を付ける

## メモを取るとき

- ユーザーが「まとめておいて」「メモ化して」のように**忘備録としての整理**を指示したときは、チャット応答だけで終わらせず、Markdown形式で `memo.md` に書き出す
  - 特定のLab（誤答の検証・特定トピックの深掘り）に紐づく内容 → `labs/NNN-topic/memo.md`
  - 特定のLabに紐づかない、試験ドメイン横断の知識 → `notes/` 配下の該当ファイル（なければ新規作成）
- 既存の `memo.md` がある場合は上書きではなく、内容を踏まえて追記・整理する
- `notes/` 配下のファイル名には、AIP-C01の出題ドメインに対応した2桁の連番プレフィックスを付ける（`NN-topic.md`）。新規ファイルを作る際も既存ファイルの番号帯に合わせる
  - `00`〜`09`: 複数ドメインにまたがる横断的な内容（プロンプトエンジニアリング共通パターン、モデル評価指標など）
  - `10`〜`19`: コンテンツ分野1 基盤モデルの統合、データ管理、コンプライアンス
  - `20`〜`29`: コンテンツ分野2 実装と統合
  - `30`〜`39`: コンテンツ分野3 AIの安全性、セキュリティ、ガバナンス
  - `40`〜`49`: コンテンツ分野4 GenAIアプリケーションの運用効率と最適化
  - `50`〜`59`: コンテンツ分野5 テスト、検証、トラブルシューティング
  - 各帯の中では既存の最大値+1を採番する（`ls notes/` で確認してから決める）
  - 帯（10個の番号）を使い切った場合は、帯番号を10倍した3桁の拡張帯（例: `20`〜`29`が枯渇 → `200`〜`209`）に同じ規則で採番を続ける
- フォーマットは既存の `memo.md` に合わせ、見出し・表・コードブロックを使い、試験直前に読み返せる密度で書く
- ユーザーが「知識が浅い」「背景知識が足りない」のように誤答の原因を**理解不足**だと述べた場合、ノートは「どの選択肢が正解か」「誤答パターンの見分け方」といった解き方の整理だけで終わらせず、**そのサービス・機能・用語自体が何であるか（仕組み・登場する要素・何のためにあるか）の解説を先に置く**。設問固有の解き方はその後に続ける補足として扱う（このリポジトリのnotesは解き方に寄りがちなので、意識して背景解説を厚くする）

## 作業終了時

- 実機デプロイを行った場合のみ、作成したAWSリソースの片付けを確認する。CFnスタックだけでなく、CLIスクリプト型Labで直接作ったリソース（S3/DynamoDB等）も対象（README「消し忘れ検出コマンド」参照）
- リポジトリ直下に一時ファイルを残さない。使い捨てファイルは `scratch/`（gitignore対象）に置く
- 誤答検証を伴うLabを完了したら、README.mdの索引テーブルに1行追加する
